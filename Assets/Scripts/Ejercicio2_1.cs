using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio2_1 : MonoBehaviour
{
    int edad,codigoPostal,numeroDeHijos,tallaCamisa,mayorEdad,minutos,dias;
    float altura,iva,peso,precio,contador;
    char genero,letra,tallaCamiseta;
    string nombre,mensaje,matricula;
    bool alumnoRepetir;
    bool mayorDeEdad;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
